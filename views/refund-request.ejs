<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css"  href="\css\aa.css">
    <script src="https://kit.fontawesome.com/1364972581.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>refund <%= concert.title %></title>
    <link rel="icon" type="image/png"  href="\media\32 x 32 icon.png">
</head>
<body>
    <nav class="navbar bg-light">
        <div class="header bg-light">
            <%- include('partials/logoTop')%>
     </nav>
     <%- include('partials/header')%>
    </div>

    <div id="container-new" class="col-6 offset-3">
<h2 class="text-center mt-3">
    <div class="line-before"></div>
    request a refund for your purchase of <%= concert.title %> 
    <div class="line-after"></div>
  </h2>


<form action="/posts/<%= concert.id %>/request-refund" method="POST">

    <label for="reason" class="mb-2">Select reason for refund</label>
    <div class="input-group mb-3">
      <div class="input-group-prepend">
        <label class="input-group-text" for="reason">Reason</label>
      </div>
      <select name="reason" required class="custom-select form-control" id="inputGroupSelect01">
          <option value="not as described">the content was not as described</option>
          <option value="no longer available">the content is no longer available (the artist removed or privated the content or deleted their account)</option>
          <option value="poor quality">the content is lacking in quality e.g. lagging, missing footage or crackling audio</option>
          <option value="fraudulent/non intentional">the purchase was fraudulent or not intentional</option>
          <option value="other">other (please add details below)</option>
      </select>
    </div>
   
    <label for="details" class="form-label">details of refund request</label>
    <textarea name="details" id="desc" cols="68" rows="20"
     class=" mb-3" required></textarea>

<div class="form-row text-center">
    <div class="col-12">
        <button type="submit" class="mt-3 rounded">Submit</button>
    </div>
 </div>
</div>
</form>

</div>
</div>
<div class="mt-auto">
<%- include('partials/footer')%>   
</div> 
</body>

<script>
  (function () {
    'use strict'
  
    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    const forms = document.querySelectorAll('.needs-validation')
  
    // Loop over them and prevent submission
    Array.prototype.slice.call(forms)
      .forEach(function (form) {
        form.addEventListener('submit', function (event) {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
  
          form.classList.add('was-validated')
        }, false)
      })
  })()
  </script>

<script>
  const alert = document.getElementById('pdAlert');
  const dismiss = document.getElementById('dismiss-pd');
  dismiss.onclick = function () {
    if (alert.style.display !== "none") {
      alert.style.display = "none";
    } else {
      alert.style.display = "block";
    }
  };
  
  </script>

<style>
    a{
    color: #3c3d3c;
    text-decoration: none;
}

a:hover{
    color: #3c3d3c;
 text-decoration: underline #1ca78b 0.3vh;
}

footer.a{
    color: blue;
}

</style>
</html>